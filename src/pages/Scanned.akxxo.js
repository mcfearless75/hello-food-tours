// Filename in this repo is historical — it syncs to the Wix page with file
// ID `akxxo`, which is the **Our Tours** page (not the QR scan landing page).
// The QR tracker now lives on `Visit Essex 2026.rbqre.js`, which is the
// page that serves /book-online (the URL the printed QR posters resolve to).

$w.onReady(function () {
    // No page-specific code yet.
});
